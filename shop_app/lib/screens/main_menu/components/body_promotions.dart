import 'package:flutter/material.dart';

class BodyPromotions extends StatelessWidget {
  const BodyPromotions({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: SafeArea(
        child: Center(
          child: Text("PROMOCIONES"),
        ),
      ),
    );
  }
}
